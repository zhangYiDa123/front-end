import { IonFocusDirective } from './ion-focus.directive';

describe('IonFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new IonFocusDirective();
    expect(directive).toBeTruthy();
  });
});
